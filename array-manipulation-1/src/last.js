/* exported last */

// Find the last item number in the list
// Find the item by the number
// return the item

function last(array) {
  var lastIndex = array.length - 1;
  var lastItem = array[lastIndex];
  return lastItem;
}
